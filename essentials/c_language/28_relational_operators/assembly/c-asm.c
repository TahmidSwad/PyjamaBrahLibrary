int swad(){
    
    char a = 127;
    char b = 3; 

    int c;

    c = a > b; // Greater than
    c = a < b; // Less than
    c = a >= b; // Greater than or equal to
    c = a <= b; // Less than or equal to
    c = a == b; // Equal to
    c = a != b; // Not equal to
}
