#include <stdio.h>

int main() {
    int a = printf("Hello, World!\n"); // prints the string and returns the number of characters printed as function output
    printf("Number of characters printed: %d\n", a); 
    return 0;
}