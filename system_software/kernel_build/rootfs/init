#!/bin/sh
# Simple init: print a message, drop to a shell
echo "Hello from the AArch64 initramfs!" > /dev/console
# Create a minimal shell environment
export PATH=/bin:/sbin:/usr/bin:/usr/sbin
# Spawn an interactive shell
exec /bin/sh
